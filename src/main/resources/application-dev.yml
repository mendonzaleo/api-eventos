spring:
  profiles:
    active: dev
  datasource:
    url: postgresql://${DB_NAME}:dFvD3CfQSvHUEBVCIyOjxPSa2xqMKkny@$DB_HOST}.oregon-postgres.render.com/${DB_USER}
    username: ${DB_USER}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
